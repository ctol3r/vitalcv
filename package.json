{
  "name": "vitalcv",
  "private": true,
  "packageManager": "pnpm@10.6.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "prisma generate --schema apps/api/backend/prisma/schema.prisma",
    "build:web": "pnpm --filter web build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "build": "turbo build"
  },
  "devDependencies": {
    "@types/node": "^20",
    "next": "15.2.8",
    "prisma": "^6.19.2",
    "turbo": "^2.7.0"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19",
      "@types/react-dom": "^19"
    }
  }
}