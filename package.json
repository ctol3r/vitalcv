{
  "name": "vitalcv",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test --filter=!@vitalcv/admin-api",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "turbo": "^2.0.0",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}