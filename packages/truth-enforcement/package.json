{
  "name": "@vitalcv/truth-enforcement",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "_comment_test": "tsconfig sets rootDir='../..' so tsc emits to dist/packages/truth-enforcement/src/test.js — not dist/test.js. Pointing the script at the real emitted path rather than changing rootDir (which would break shared-source imports).",
    "test": "node dist/packages/truth-enforcement/src/test.js"
  },
  "dependencies": {
    "@vitalcv/trust-contract": "file:../trust-contract"
  }
}
