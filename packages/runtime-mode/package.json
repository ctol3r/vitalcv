{
  "name": "@vitalcv/runtime-mode",
  "version": "1.0.0",
  "description": "Runtime mode detection and enforcement for test/dev/prod environments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "dev": "tsc -b tsconfig.json --watch",
    "clean": "rm -rf dist",
    "test": "vitest"
  },
  "keywords": [
    "runtime",
    "mode",
    "test",
    "environment",
    "enforcement"
  ],
  "author": "VitalCV Engineering",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
