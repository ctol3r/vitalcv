{
  "name": "@chai-vc/rate-limiter",
  "version": "1.0.0",
  "description": "Rate limiting middleware with in-memory and Redis backends for chai-vc platform services.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json"
  },
  "dependencies": {
    "@vitalcv/runtime-mode": "workspace:*",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17"
  },
  "peerDependencies": {
    "redis": "^4.6.0"
  },
  "peerDependenciesMeta": {
    "redis": {
      "optional": true
    }
  }
}
