# Railway CLI upload overrides. The CLI's ignore matcher does not honor the
# root .gitignore's leading-slash anchor on /artifacts/, so it silently drops
# apps/web/components/artifacts — tracked application source — from uploads.
# Re-include it explicitly.
!apps/web/components/artifacts
!apps/web/components/artifacts/**
