{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.6.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.6.1 --activate && pnpm --filter web build"
}