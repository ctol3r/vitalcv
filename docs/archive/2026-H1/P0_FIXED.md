# P0 Fixed

The launch wedge now has the highest-priority public issues addressed.

## Fixed

- Homepage CTA wording no longer uses vague request language for the public wedge.
- Signup copy now reads as a direct account-creation path instead of an internal foundation note.
- Runtime-health labels are shorter and easier to read operationally.
- Passport trust posture copy is calmer and more recruiter-friendly.
- Public static rendering is enabled for the safe launch surfaces.
- The employer demo page and contact intake page are now static-rendered.
- The clinician onboarding surface is now static-rendered.

## Verified

- Typecheck passes.
- The focused web test slice passes.
- The production build passes.
- The build route table shows the public wedge is static where intended.

## Remaining non-code blocker

- The live production domain still needs to be confirmed on the canonical `vcv-web` runtime before a broad relaunch announcement.

## Remaining non-blockers

- Dynamic passport hydration.
- Dynamic onboarding continuation.
- Dynamic holder/workspace flows.
- Employer operator surfaces outside the public wedge.
