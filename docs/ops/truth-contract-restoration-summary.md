# Truth-Contract Restoration — Wave 1 Task Package
**Wave:** Wave 1  
**Date:** 2026-05-07  
**Classification:** GUARDED  
**Audit doc:** docs/ops/truth-contract-restoration-audit.md  
**Generated by:** OpenClaw (orchestrator — does not implement)  
**Implements:** Claude Code Terminal

---

## Branch Name

```
truth/wave1-truth-contract-restoration
```

## PR Title

```
truth(copy): Wave 1 truth-contract restoration — remove unsupported claims, NPDB, PECOS freshness, NCQA, instant-implication language
```

---

## Scope Lock

**Files to change:**
```
apps/web/app/HomePageClient.tsx
apps/web/components/sandbox/EmployerReviewDashboard.tsx
apps/web/components/marketing/AcceptanceNetwork.tsx
apps/web/components/employer/ApplyWidgetConfig.tsx
apps/web/components/clinician/FocusModeQR.tsx
apps/web/components/marketing/LedgerTicker.tsx
apps/web/components/marketing/BentoGrid.tsx
apps/web/__tests__/truth-contract-wave1.test.ts   (new)
```

**Files NOT to change:**
```
apps/web/middleware.ts                              (auth — not in scope)
apps/web/prisma/schema.prisma                       (schema — never in scope)
apps/web/lib/issuer-verification/                   (trust chain — not in scope)
packages/crs/                                       (CRS engine — not in scope)
packages/source-adapters/                           (adapters — not in scope)
apps/web/app/HomePageClient.tsx NPI submit logic    (only SOURCES/TIER_LADDER/footer sections)
apps/web/components/review/EmployerCockpit.tsx      (already honest — do not touch)
apps/web/components/simulation/TrustEngineTerminal.tsx  (already has correct comments)
```

---

## Claude Code Terminal Execution Prompt

```
You are Claude Code Terminal implementing Wave 1: Truth-Contract Restoration for VitalCV.

CONTEXT:
Read before starting:
  cat CLAUDE.md
  cat docs/ops/truth-contract-restoration-audit.md
  git log --oneline origin/main -3

This is a GUARDED operation — copy and demo-marker changes only.
NO auth changes. NO schema changes. NO CRS changes. NO source adapter changes.

SETUP:
git fetch origin main
git worktree add -b truth/wave1-truth-contract-restoration /tmp/vitalcv-wave1-truth origin/main
cd /tmp/vitalcv-wave1-truth
pnpm install
pnpm turbo run build --filter='!@vitalcv/web'   # pre-build workspace deps

IMPLEMENT EXACTLY THESE CHANGES:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 1: apps/web/app/HomePageClient.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find the TIER_LADDER constant. Change the T3 tier example:
  FROM: example: 'NPPES · OIG/LEIE · PECOS · NPDB.'
  TO:   example: 'NPPES · OIG/LEIE · PECOS.'
  REASON: NPDB is not integrated. Remove it from the example.

Find the SOURCES constant. Change PECOS entry:
  FROM: { code: 'PECOS', ... chip: 'Live',  freshness: 'Updated daily' }
  TO:   { code: 'PECOS', ... chip: 'Gated', freshness: 'Quarterly snapshot' }
  REASON: PECOS is a quarterly CMS data release, not a live daily API.

Find the "Conforms to" text block (around line 371):
  FROM: Conforms to{' '}
          <span ...>NCQA CR §3</span>,{' '}
          <span ...>W3C VC 2.0</span>,{' '}
          <span ...>OpenID4VCI</span>
  TO:   Architecturally aligned with{' '}
          <span ...>W3C VC 2.0</span>{' '}
          and{' '}
          <span ...>OpenID4VCI</span>
  REASON: VitalCV is not NCQA-certified. Remove "Conforms to NCQA CR §3".

Find the footer (around line 418):
  FROM: Delegated credential verification infrastructure · NCQA CR §3 · §4.2
  TO:   Delegated credential verification infrastructure · W3C VC 2.0 · OpenID4VCI
  REASON: NCQA CR §3 implies certification. Replace with standards we demonstrably implement.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 2: apps/web/components/sandbox/EmployerReviewDashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

At the top of the file, add a structural simulation marker constant:
  const SIMULATION_DISCLAIMER =
    '⚠ Simulation data — no real verification has occurred. Sources marked CHECKED are illustrative only.';

Find ALL occurrences of:
  - 'SAM.gov check in progress.' → 'Federal exclusion (not yet integrated — simulation only)'
  - 'SAM.gov clear. No active exclusions.' → 'Federal exclusion (not yet integrated — simulation only)'
  - 'SAM.gov clear.' → 'Federal exclusion (not yet integrated — simulation only)'
  - 'ABMS certification verified.' → 'Board certification (not yet integrated — direct PSV required)'
  - 'DEA registration active.' → 'DEA registration (not yet integrated — direct verification required)'
  - 'NPDB query submitted...' → 'Adverse actions (not yet integrated — simulation only)'
  - 'NPDB: No adverse actions reported.' → 'Adverse actions (not yet integrated — simulation only)'
  - Name: "Federal Exclusions" → Name: "Federal Exclusion (simulation)"
  - Name: "Board Certification" (with ABMS details) → Name: "Board Certification (simulation)"
  - Name: "NPDB" → Name: "Adverse Actions (simulation)"

Remove from source name filter arrays any reference to "NPDB", "Federal Exclusions",
"Sanctions" that route to SAM.gov output. Replace with simulation-safe alternatives.

Find the component's main render return. At the top of the rendered UI,
add a visible simulation disclaimer banner using SIMULATION_DISCLAIMER:
  <div className="mb-4 rounded-md border border-amber-300 bg-amber-50 px-4 py-2 text-xs text-amber-800">
    {SIMULATION_DISCLAIMER}
  </div>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 3: apps/web/components/marketing/AcceptanceNetwork.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find (line ~43):
  FROM: 'Validate instantly'
  TO:   'Validate on demand'
  REASON: "Instantly" implies zero-latency verification. VitalCV does source lookups, which take time.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 4: apps/web/components/employer/ApplyWidgetConfig.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find (line ~185):
  FROM: 'Share verified credentials instantly. No paperwork required.'
  TO:   'Share source-backed credential packets. Structured, auditable, no paper forms.'
  REASON: "verified credentials instantly" is misleading — credentials are source-checked,
           not verified-in-the-legal-sense, and not instant.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 5: apps/web/components/clinician/FocusModeQR.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find (line ~76):
  FROM: 'Scan to verify credentials instantly'
  TO:   'Scan to review credential evidence'
  REASON: "verify credentials instantly" overclaims — the scan presents a packet for review,
           not a live verification event.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 6: apps/web/components/marketing/LedgerTicker.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find the event entry for Nursys (line ~19):
  FROM: { hash: 'evt:e573…01', event: 'Nursys: License Standing Confirmed', accent: '...' }
  TO:   { hash: 'evt:e573…01', event: 'State Board: Access gated (institutional credential required)', accent: 'text-[var(--vt-text-muted)]' }
  REASON: Nursys requires REAL_NURSYS_ENABLED=true. It cannot appear as "Confirmed" in any
           public surface — this implies a real check ran. Use a neutral access-gated label.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 7: apps/web/components/marketing/BentoGrid.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find the sources array (line ~123):
  FROM: const sources = ['NPPES', 'CA-BRN', 'OIG/LEIE', 'Nursys']
  TO:   const sources = ['NPPES', 'OIG/LEIE', 'State Board (gated)']
  REASON: CA-BRN is not a canonical source name used in the system.
           Nursys must be labeled "gated" — it is not live without institutional access.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 8: apps/web/__tests__/truth-contract-wave1.test.ts  (NEW)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create a new test file. Pattern: import the relevant source constants/strings and
assert they do not contain banned strings or unsupported vendor claims.

Include tests for:
  - TIER_LADDER T3 example does not contain 'NPDB'
  - SOURCES PECOS entry has chip !== 'Live'
  - SOURCES PECOS entry has freshness !== 'Updated daily'
  - AcceptanceNetwork copy does not contain 'instantly'
  - FocusModeQR copy does not contain 'instantly'
  - ApplyWidgetConfig copy does not contain 'instantly'
  - LedgerTicker events do not contain 'Nursys: License Standing Confirmed'
  - BentoGrid sources do not include 'Nursys' without '(gated)' qualifier
  - EmployerReviewDashboard simulation data contains SIMULATION_DISCLAIMER

VERIFICATION:
After all changes:

1. git diff --staged | grep -iE "NPDB|SAM\.gov|ABMS certification verified|DEA registration active|instantly|Conforms to NCQA|NCQA CR §3|hire instantly"
   → Must return ZERO matches. If any match: fix before committing.

2. pnpm typecheck
   → Must pass clean.

3. pnpm lint
   → Must pass clean.

4. pnpm --filter @vitalcv/web exec vitest run __tests__/truth-contract-wave1.test.ts
   → All tests must pass.

5. pnpm --filter @vitalcv/web build
   → Must build cleanly.

OPEN PR:
gh pr create \
  --title "truth(copy): Wave 1 truth-contract restoration — remove unsupported claims, NPDB, PECOS freshness, NCQA, instant-implication language" \
  --body "$(cat docs/ops/truth-contract-restoration-summary.md | head -80)" \
  --base main

DO NOT MERGE. Generate the Codex audit prompt and hand off.
```

---

## Codex Audit Prompt

```
Audit PR: truth/wave1-truth-contract-restoration against origin/main.
Wave: Wave 1 — Truth-Contract Restoration
Authority: CLAUDE.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AUDIT 1 — IMPLEMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Confirm each of the following specific changes was made:

HomePageClient.tsx:
  □ T3 tier example no longer contains 'NPDB'
  □ PECOS source entry: chip is NOT 'Live'
  □ PECOS source entry: freshness is NOT 'Updated daily'
  □ "Conforms to NCQA CR" does not appear anywhere in the file
  □ Footer "NCQA CR §3" reference does not appear
  □ No new data fetching, no auth logic, no scoring logic introduced

EmployerReviewDashboard.tsx:
  □ All instances of 'SAM.gov' are replaced with simulation-safe language
  □ All instances of 'ABMS certification verified' are replaced
  □ All instances of 'DEA registration active' are replaced
  □ All instances of 'NPDB' as a real-source reference are replaced
  □ A structural simulation disclaimer constant is defined at the top
  □ The disclaimer is rendered visibly in the component output

AcceptanceNetwork.tsx:
  □ 'Validate instantly' does not appear anywhere in the file

ApplyWidgetConfig.tsx:
  □ 'Share verified credentials instantly' does not appear anywhere in the file

FocusModeQR.tsx:
  □ 'Scan to verify credentials instantly' does not appear anywhere in the file

LedgerTicker.tsx:
  □ 'Nursys: License Standing Confirmed' does not appear anywhere in the file
  □ Nursys replacement text does not imply a completed verification

BentoGrid.tsx:
  □ 'Nursys' does not appear without a '(gated)' or access qualifier
  □ 'CA-BRN' as a source name does not appear

Test file:
  □ truth-contract-wave1.test.ts exists and imports from the changed files
  □ Tests assert the specific strings are absent
  □ Tests pass (confirm from vitest output in PR description)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AUDIT 2 — DIFF
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify scope is bounded:
  □ ONLY the 8 listed files are changed (7 product + 1 test)
  □ apps/web/middleware.ts is NOT changed
  □ apps/web/prisma/schema.prisma is NOT changed
  □ packages/ is NOT changed
  □ apps/web/lib/issuer-verification/ is NOT changed
  □ No new imports are added to product files (only string changes + one constant)
  □ No new routes are introduced
  □ No logic changes in HomePageClient.tsx outside the SOURCES/TIER_LADDER/footer sections
  □ No removal of structural code — only copy changes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AUDIT 3 — COPY/TRUTH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan ALL changed files for:

MUST NOT APPEAR (any match = FAIL):
  □ "automatically verified"
  □ "guaranteed verification"
  □ "complete credentialing"
  □ "instant credentialing"
  □ "legally accepted"
  □ "risk transferred"
  □ "final verification without review"
  □ "source confirmed before response"
  □ "certified compliant"
  □ "HIPAA compliant"
  □ "SOC2 certified"
  □ "hire instantly"
  □ "instantly" (in any user-facing string)
  □ "Conforms to NCQA"
  □ "NCQA CR" (as a certification claim)
  □ "ABMS certification verified"
  □ "SAM.gov clear"
  □ "NPDB" (as a real data source in user-visible output)
  □ "DEA registration active"
  □ "Nursys" without a gated/access-required qualifier

MUST APPEAR in EmployerReviewDashboard.tsx:
  □ "simulation" OR "Simulation" (in the disclaimer)
  □ "not yet integrated" OR "not real" (in the data strings)

MUST NOT be newly introduced:
  □ Any bare "Verified" as a standalone status label
  □ Any claim that Nursys has run or confirmed
  □ Any claim that PECOS is checked in real time

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERDICT REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SAFE = All □ boxes above confirmed. No violations found.
FAIL = Any □ failed. List the specific file:line for every failure.

Do not issue SAFE unless every check passes.
```

---

## Rollback Instructions

If a post-merge regression is found:

```bash
# Revert the PR
gh pr revert <PR_NUMBER> --title "revert: truth/wave1-truth-contract-restoration"
# The revert creates a new PR — Codex audit the revert before merging it
# Revert Codex audit: confirm only the Wave 1 changes are reversed,
# no other files affected, no auth guards dropped
```

If a specific violation slipped through (banned string re-introduced):
```bash
# Hotfix flow — surgical change, abbreviated Codex (diff + copy/truth only)
git worktree add -b hotfix/wave1-followup /tmp/vitalcv-w1-hotfix origin/main
# Fix the specific string
# Abbreviated Codex: diff audit + copy/truth audit only
# Merge immediately
```

---

## Completion Criteria

Wave 1 is complete when:
- [ ] All 23 violations (V-01 through V-23) are resolved in merged code
- [ ] vitest truth-contract-wave1.test.ts passes on origin/main
- [ ] banned-strings CI gate (PR #225) is also merged (prerequisite)
- [ ] Completion board row "Product truth" moves from current score upward
- [ ] Knowledge Trust Graph is updated if any new trust boundary is formalized

---

## Remaining Known Truth Risks (Intentionally Deferred)

| File | Violation | Reason deferred |
|---|---|---|
| `apps/web/components/employer/EmployerDashboard.tsx:819` | "instantly" fragment | Medium severity, low visibility; Wave 2 |
| `apps/web/components/clinician/WalletDashboard.tsx` | NCQA-Aligned in methodologyVersion | Internal version string, not public certification claim |
| `apps/web/components/marketing/HeroAppPreview.tsx` | Bare `status: 'Verified'` in demo data | Need to confirm parent structural label before modifying |
| `apps/web/components/marketing/HomeSections.tsx:336` | "working in real time" | Borderline — NPPES lookups are real-time; acceptable |
| `apps/web/components/trust-state/types.ts` | 'NPDB' and 'ABMS' in type union | Internal type catalog — not public-facing copy |
