'use client';

/**
 * VerifierCommandCenter — Wave 10: Employer Command Center
 * Stub component (Wave 11 pre-existing build-fix).
 */
export function VerifierCommandCenter() {
  return (
    <div className="rounded-2xl border border-border bg-card p-10 text-center text-muted-foreground">
      <p className="text-sm">Command Center — coming soon.</p>
    </div>
  );
}
