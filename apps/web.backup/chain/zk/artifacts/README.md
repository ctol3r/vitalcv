# SNARK Artifacts

Drop the compiled Groth16 assets produced from `circuits/event-ledger.circom`
in this directory. The prover expects the following filenames by default:

- `event-ledger.wasm`
- `event-ledger.zkey`
- `event-ledger.vkey.json`

These files are intentionally git-ignored because they are environment-specific.


