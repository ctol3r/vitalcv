# Homepage v4 — the founder's design of record (amendment F)

**Founder directive, 2026-08-16, quoted verbatim:** *"Implement: VitalCV Homepage v4.html"*

This directory holds the founder's design VERBATIM, for citability (EC-21):

- `index.html` — the founder's `VitalCV Homepage v4.html`, byte-for-byte as
  handed off (markup + styles; the original's interactive resolve script and
  tweaks-panel scaffolding are summarized in its trailing comment). Its
  committed defaults: motion `ambient`, illustration `filled`, accent
  `#4338CA`, paper `#EDEAE3`, headline **"Get hired faster. Start working
  sooner."**
- `vitalcv-ill-v4.css` — the v4 illustration kit (evidence-geometry classes,
  single-shot/ambient motion, reduced-motion kill), byte-for-byte.

**This artifact is raw material, not shipped truth.** The production port
lives in `apps/web/components/home/easy/` under amendment F of
`docs/design/VITALCV_EXPERIENCE_CONSTITUTION.md`, which records — one by one —
every place where standing law required the port to differ from this file
(vocabulary, masked NPI, real source lanes, producible states, the dropped
sixth legend state, single-shot motion, the unported glass rail, and the
claims-gate adjustments to the headline and axis). Nothing in this directory
is customer-facing, and nothing in it may be cited as authority for the
strings standing law corrected.
