// Prisma schema for Chai VC Platform
// Defines a simple Credential model used in GraphQL resolvers

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Credential {
  id        String   @id @default(cuid())
  name      String
  issuer    String
  issuedAt  DateTime @default(now())
}
