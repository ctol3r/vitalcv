# Review captures — PR #1063 (head 8e96da21)

Generated for the founder final-pixel gate. **This branch is never merged.**
It exists so the PR comment can embed images without committing generated
binaries into main.

Stills are the production build of PR #1063 at head `8e96da21`.
The resolved-NPI views use a mocked API payload of the real shape (this
machine has no backend); the un-mocked local behaviour is the honest
system-state capsule.
